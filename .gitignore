node_modules/
dist/
.env
.env.local
.env.*.local
__pycache__/
*.pyc
.venv/
venv/
.DS_Store
sync/.mfp-profile/
sync/.garmin-tokens/
.agents/
.claude/skills/
web/.env.local
.wrangler/

# Local-only Cloudflare Pages Function secrets for `wrangler pages dev`.
# Never committed: these are the same values held as Cloudflare secrets in production.
.dev.vars
web/.dev.vars
